import { UIBase } from "./uibase.js";

/**
 * 
 * @class
 * @classdesc 数据迁移等待页面整体UI渲染功能类
 * @extends UIBase
 * @author 陈宇鸿 <785344575@qq.com>
 */
class UItransPage extends UIBase {
    /**
     * 是构造器啊
     * @constructor
     */
    constructor(uiManager) {
        super();
        this.uiManager = uiManager;
    }

    _bindEvent() {

    }

    _render() {
        return `
            
        `;
    }
}